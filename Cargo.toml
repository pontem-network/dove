[workspace]
members = [
    "language_server",
    "executor",
    "dove",
    "lang",
    "lang/compat",
    "lang/disassembler",
    "resources",
    "common/git-hash",
]

[profile.release]
lto = "thin"
opt-level = 's'
codegen-units = 1
rpath = false
