module M {
   resource struct R {
           f: u64
   }

   fun v64(): u64 {
        1
   }

    fun v128(): u128 {
        1
    }

   fun v8(): u8 {
       1
   }

   fun t0() {
       1 + 0;
       0 + (1: u8);
       (0: u8) + 1;
       0 + (1: u128);
       (0: u128) + 1;
       (0) + (1);
   }

   fun t01() {
       0 + v64();
       v8() + (1: u8);
       (0: u8) + v8();
       v128() + (1: u128);
       (0: u128) + v128();
       (v64()) + (1);
   }

   fun t1() {
      1 + v64() * 2;
      1 + (v64() * 2);
      (v64() + 1) * 2;
      2 * 2 / 2;
      v64() % 2;
      v64() + 1 % v64();
      v64() / 1 % v64();
      v64() * 1 % v64();
      v64() * 1 ^ v64();
      v64() & 1;
      v64() | 1;
   }

   fun t11() {
      1 + 1 * 2;
      1 + (1 * 2);
      (1 + 1) * 2;
      2 * 2 / 2;
      1 % 2;
      1 + 1 % 2;
      1 / 1 % 2;
      2 * 1 % 2;
      2 * 1 ^ 2;
      1 & 1;
      1 | 1;
   }

   fun c(c: bool, c1: bool, c3: bool): bool {
        c && (!c1 || c3)
   }

   fun c1(c: bool, c1: bool): bool {
        c || !c1
   }


   fun t2(b: bool, i: u64, i1: u64) {
        !true;
        !b;
        true && false;
        b && !b;
        true || false;
        b || !b;
        1 > 2;
        i > i1;
        1 >= 2;
        i >= i1;
        1 < 2;
        i < i1;
        1 <= 2;
        i <= i1;
   }

   fun t3(i: u64, i1: u8) {
       0 << 1;
       i << i1;
       0 >> 1;
       i >> i1;
   }
}