[workspace]
members = [
    "dove",
    "lang",
    "lang/compat",
    "net",
    "common/git-hash",
    "resource-viewer",
    "subxt-client"
]
exclude = [ "libs", "libs/subxt","libs/hash_project"]

[profile.release]
lto = "thin"
opt-level = 's'
codegen-units = 1
rpath = false
