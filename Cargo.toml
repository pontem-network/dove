[workspace]
members = [
    "dove",
    "lang",
    "lang/compat",
    "net",
    "common/git-hash",
    "resource-viewer"
]

[profile.release]
lto = "thin"
opt-level = 's'
codegen-units = 1
rpath = false
